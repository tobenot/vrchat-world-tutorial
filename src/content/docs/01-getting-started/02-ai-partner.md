---
title: 2. AI 是你的创作搭档
description: 学会把 AI 当成旁边的搭档，用它查资料、拆问题、写草稿，同时保留自己的判断。
---

# 2. AI 是你的创作搭档

你打开 Unity，看到一整面面板。你搜一个报错，出来十几个答案。你问朋友，朋友说「这个简单」，然后发来一段你看不懂的脚本。

这时候 AI 很有用。

它可以陪你把问题拆小，可以把英文文档翻成你能读下去的话，可以帮你写第一版 UdonSharp 脚本，也可以在你卡住的时候给你几个排查方向。

但你要先记住一件事：AI 给的是建议，Unity 和 VRChat SDK 给的是结果。最后以你本地跑出来的东西和官方文档为准。

## 把 AI 放到正确的位置

我建议你把 AI 当成旁边那位会查资料、会写草稿、很有耐心的搭档。

它能帮你把一个大问题拆成几个小步骤。能把英文文档解释成顺口的中文。能根据你的目标写一份脚本草稿。能看到报错之后列出可能原因。能帮你把「我想做一个效果」翻译成 Unity 里的对象、组件和脚本。

但它也会犯错。它可能把旧版 SDK 的做法当成现在还能用。可能编造一个看起来很真的组件名。可能写出标准 C# 能跑、UdonSharp 里却会炸的代码。可能漏掉多人同步、权限和平台限制这些麻烦的点。也可能给你一个太大的方案，让你第一步就动不了。

所以我们用 AI，但不把方向盘交给它。

## 一个好问题长什么样

问 AI「VRChat 怎么做门」，它可能给你一整套门系统：动画、按钮、音效、同步、权限、UI 全都有。看起来很厉害，落地时你连第一个脚本挂哪里都不知道。

更好的问法是这样的：

```text
我在做 VRChat World，用 Unity 和 UdonSharp。
我现在想做一扇最简单的门：玩家点击门，门旋转 90 度打开。
先不要做多人同步，也不要做动画曲线。
请你给我一个最小可运行步骤：
1. 场景里要建哪些物体
2. 每个物体要挂哪些组件
3. UdonSharp 脚本写什么
4. 我应该怎样测试
```

这里面有几个让 AI 变靠谱的关键：你说清了环境（VRChat World、Unity、UdonSharp），限制了范围（最简单的门、先不做同步），要求了具体结果（最小可运行步骤），还指定了输出结构。

你给 AI 的边界越清楚，它越像搭档。你只说「帮我做一个超酷的门」，它就开始自由发挥。

## 用 AI 读官方文档

VRChat 和 Unity 的官方文档很重要，但新手直接读会累。

你可以把官方页面链接发给 AI，然后这样问：

```text
请只根据这个官方页面回答，不要补充你记忆里的内容：
https://creators.vrchat.com/worlds/components/vrc_scenedescriptor/

我刚开始做 VRChat World。
请用新手能懂的话解释 VRC Scene Descriptor 是什么，为什么每个世界都需要它。
最后列出我现在最该关心的 3 个字段。
```

这种问法有两个好处。第一，它把 AI 的回答限制在官方资料附近。第二，它让文档从「一整页术语」变成「我眼下该懂什么」。

以后你碰到 `VRC_Pickup`、`VRCObjectSync`、`VRCSpatialAudioSource` 这些组件，也可以用同样的方式读。

## 用 AI 拆报错

报错不要只复制最后一行。尽量给完整上下文：

```text
我在 Unity 里做 VRChat World，使用 UdonSharp。
我点击 Build & Test 后失败了。
下面是 Console 里的报错，请你帮我判断可能原因，并按「最可能」「次可能」「需要更多信息」分类。

[把完整报错贴在这里]

请不要直接让我重装 Unity。先从项目设置、组件缺失、脚本编译错误这些方向排查。
```

这个问法能避免 AI 一上来就给你大动作。很多 Unity 问题最后只是少挂了一个组件、脚本没编译过、场景里少了 Descriptor。先从小地方查。

## 用 AI 写脚本草稿

让 AI 写脚本的时候，要求它写小一点。

比如你想做一个按钮开灯：

```text
请写一个 UdonSharp 脚本：
- 挂在一个可点击按钮上
- 玩家 Interact 时切换一盏 Light 的 enabled 状态
- 先不做多人同步
- 代码里用清楚的字段名
- 最后告诉我在 Unity Inspector 里要把哪个对象拖到哪个字段
```

拿到代码后先别急着复制粘贴。看看它的类有没有继承 `UdonSharpBehaviour`，交互方法是不是 `public override void Interact()`，有没有说清楚你需要在 Inspector 里把灯拖进公开字段，有没有提醒你这个脚本只控制本地效果、其他玩家暂时看不到。

如果哪里没说清楚，继续追问。追问比重开一个新对话更好，因为上下文还在。

## 从第一天开始做记录

我建议你给每个小功能留一段实验记录。不用正式，像这样就够：

```text
目标：点击按钮让灯开关
今天做到：本地可以开关，其他玩家还看不到
关键组件：Light、Collider、UdonSharp 脚本
卡住的点：按钮没有 Collider 时无法点击
下次要查：多人同步变量、SendCustomNetworkEvent
```

这段记录以后很值钱。你做第二个世界的时候，会感谢现在的自己。

AI 也可以帮你整理记录。把聊天记录、报错和最终做法贴给它，让它输出成「问题、原因、解决步骤、以后注意」四段。

---

下一章，我们开始装工具。那一步有点像整理工位：先把该用的东西放好，后面做世界才不会到处找螺丝刀。

## 本章参考

- [VRChat Creator Docs](https://creators.vrchat.com/)
- [VRChat SDK](https://creators.vrchat.com/sdk/)
- [VRC Scene Descriptor](https://creators.vrchat.com/worlds/components/vrc_scenedescriptor/)
- [UdonSharp 文档](https://udonsharp.docs.vrchat.com/)
